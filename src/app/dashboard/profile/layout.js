// src/app/dashboard/profile/layout.js
export const metadata = {
  title: 'User Profile - School Management',
  description: 'View and manage user profile information.',
};

export default function ProfileLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}